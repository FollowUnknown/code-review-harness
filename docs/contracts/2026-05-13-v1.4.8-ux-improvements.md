# Contract: v1.4.8 交互体验优化

> 日期: 2026-05-13
> 状态: completed
> 类型: Platform Task (G4 文档治理) + Business Task (G1/G2/G3/G5)
> 版本: v1.4.8

## 背景

v1.4.7 提交后自动清理机制已上完。当前代码库中存在一些历史积累的交互缺陷和废弃代码：Product Line 需手动选择、评审完成信息不完整、local 模式已无人使用、部分 docs 内容过时、/memory 页面未被有效利用。

## 范围

### G1 — Product Line 默认选中

- 文件：`src/client/pages/RequirementReviewPage.tsx`
- 改动：`useEffect` 加载 productLines 后，若列表非空且无恢复场景的已有选择，自动 `setProductLine(list[0].id)`
- 不改变：恢复场景（中断恢复、job 恢复、checkpoint 恢复）的已有 productLine 设置逻辑

### G2 — 评审完成提示补全需求信息

- 文件：`src/client/pages/RequirementReviewPage.tsx`
- 改动：绿色完成提示区（`reviewId &&` 区块）从纯文字改为带上需求标题和 Product Line

### G3 — local 模式全面移除

涉及删除：
- 前端：`src/client/pages/LocalReviewPage.tsx`
- 路由：`src/client/App.tsx` 移除 import、Route、NavLink
- 后端路由：`src/server/routes/review-local.ts`
- 后端路由注册：`src/server/index.ts` 移除 import 和 `app.use`
- 服务：`src/server/services/local-scan/` 整个目录
- 类型：`src/shared/types.ts` 中 `ReviewType` 移除 "local"
- 依赖引用：`src/server/services/reviewer.ts`、`review-store.ts` 等文件中 local 相关逻辑
- 测试文件清理
- 文档：`docs/architecture/local-code-scan.md` 标记废弃

### G4 — Docs 过期审计

审查并标记以下目录状态：
- `docs/product/index.md` — 严重过时（2026-04-21）
- `docs/standards/testing.md` — 可能过时（2026-04-21）
- `docs/retrospectives/` — 无5月回顾

不重写内容，只标记状态和添加指向最新版本的链接。

### G5 — /memory 页面使用分析

分析 `/memory` 页面未被有效使用的原因，输出分析报告。

## 不做

- 不修改 /memory 页面本身（G5 只分析不实现）
- 不改 Product Line 下拉 UI 样式
- 不改评审流程的核心逻辑
- 不重构 local 模式删除后的代码结构（只做删除）

## 验收标准

| # | 标准 | 关联 G |
|---|------|--------|
| 1 | 进入需求评审页，Product Line 下拉自动选中第一个可用项 | G1 |
| 2 | 恢复场景（中断/job/checkpoint）的 Product Line 不受影响 | G1 |
| 3 | 评审完成后，"Requirement review complete" 文字带需求标题和 Product Line | G2 |
| 4 | local 模式在导航栏、路由、后端、服务端代码中完全移除 | G3 |
| 5 | TypeScript 编译通过，无 local 相关引用遗留 | G3 |
| 6 | 过时 docs 已标记状态和链接指向 | G4 |
| 7 | /memory 页面分析报告输出 ≥3 条改进建议 | G5 |

## 执行顺序

G1 → G2 → G3 → G4 → G5

每个 G 完成后在 session 记录。G3 涉及面广，单独审查。
