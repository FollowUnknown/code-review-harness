# 2026-05-08 会话

## 今日目标
- Vite proxy 配置 + API_BASE 改为相对路径（已完成）
- Export by File "Unknown File" → "通用问题"

## 会话记录

### 09:00 - Vite proxy + API_BASE 改造
- 将 19 个客户端文件的 `API_BASE` 从 `http://localhost:3001` 改为 `""`（相对路径）
- 由 vite.config.ts 的 proxy 转发 `/api` 到 `localhost:3001`
- 效果：localhost 和 IP 访问均可正常工作

### 09:30 - 修复导出中 "Unknown File" 显示
- 将空 file 字段的兜底文案从 "Unknown File" 改为 "通用问题"

### 01:06 - [auto] src/client/components/ReviewResult.tsx
- Edit on src/ 文件（自动记录）
